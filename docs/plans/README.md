# Execution Plans

Plans are Git-native working memory for complex work. Use an ephemeral plan for
bounded single-session work. For work that spans sessions, coordinates
contributors, has dependencies, or needs recovery steps, create one plan under
`active/` from `docs/templates/exec-plan.md` and move it to `completed/` after
validation.
