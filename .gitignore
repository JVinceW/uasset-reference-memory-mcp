# macOS Finder metadata.
.DS_Store

# JavaScript dependencies for local web UI packages.
node_modules/
*.tsbuildinfo

# Local web UI build and test output.
desktop-dist/
test-results/
tsconfig.tsbuildinfo
dist/
spotcheck.mjs
spot-*.mjs

# npm pack artifacts.
*.tgz

# Tool index output (when indexing a project that lives inside this repo).
.asset-memory/

# Local Git worktrees used for isolated feature development.
.worktrees/
